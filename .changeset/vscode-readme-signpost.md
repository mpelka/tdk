---
"tdk-vscode": patch
---

Shrink the extension README to a signpost pointing at the new VS Code extension guide on the docs site. The feature walkthrough — compile preview, form preview, the two-slot TDK Trace panel, dry-run in Backstage, plain-YAML preview, and the Test Explorer — now lives in one canonical page (`apps/docs/guide/vscode.md`), so the README no longer duplicates it. The develop and publish notes stay in the README.
